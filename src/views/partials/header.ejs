<!-- Navbar con Bootstrap -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <% if (!locals.isLogged) { %>
            <a href="/">
                <button class="btn btn-dark fs-2" type="button">
                    Hola!!!
                </button>
            </a>
        <% } %>
        <% if (locals.isLogged) { %>
            <div class="navbar"> <!-- Deja la clase "navbar" sin cambios -->
                <div class="dropdown">
                    <button class="btn btn-dark dropdown-toggle fs-2" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Hola <%= locals.userLogged.nombre %>
                    </button>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="/users/profile">Profile</a>
                        <a class="dropdown-item" href="/users/logout">Logout</a>
                        <a class="dropdown-item" href="/">Home</a>
                    </div>
                </div>
            </div>
        <% } %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto"> <!-- Usa "ms-auto" para alinear a la derecha -->
                <% if (locals.isLogged) { %>                    
                    <li class="nav-item">
                        <% if (locals.userLogged.admin == "full-admin") { %>
                            <div class="dropdown">
                                <a class="nav-link fs-3 dropdown-toggle" href="#" id="navbarDropdownCalendario" role="button"
                                    data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Calendario
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdownCalendario">
                                    <a class="dropdown-item" href="/calendario/Mansilla">Mansilla</a>
                                    <a class="dropdown-item" href="/calendario/Las-Heras">Las Heras</a>
                                    <a class="dropdown-item" href="/calendario/Corrientes">Corrientes</a>
                                </div>
                            </div>
                        <% } else if (locals.userLogged.admin == "corrientes-admin") { %>
                            <a class="nav-link fs-3" href="/calendario/Corrientes">Calendario</a>
                        <% } else { %>
                            <a class="nav-link fs-3" href="/calendario/Las-Heras">Calendario</a>
                        <% } %> 
                    </li>
                    <li class="nav-item">
                        <% if (locals.userLogged.admin == "full-admin") { %>
                            <div class="dropdown">
                                <a class="nav-link fs-3 dropdown-toggle" href="#" id="navbarDropdownFacturacion" role="button"
                                    data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Facturacion
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdownFacturacion">
                                    <a class="dropdown-item" href="/facturacion/Mansilla">Mansilla</a>
                                    <a class="dropdown-item" href="/facturacion/Las-Heras">Las Heras</a>
                                    <a class="dropdown-item" href="/facturacion/Corrientes">Corrientes</a>                                 
                                </div>
                            </div>
                        <% } else if (locals.userLogged.admin == "corrientes-admin") { %>
                            <a class="nav-link fs-3" href="/facturacion/Corrientes">Facturacion</a>
                        <% } else { %>
                            <a class="nav-link fs-3" href="/facturacion/Las-Heras">Facturacion</a>
                        <% } %>  
                    </li>
                <% } %>
                <% if (!locals.isLogged) { %>
                    <li class="nav-item">
                        <a class="nav-link fs-3" href="/users/login">Login</a>
                    </li>       
                <% } %>
                <% if (locals.isLogged && locals.userLogged.admin == "full-admin") { %>
                    <li class="nav-item">
                        <a class="nav-link fs-3" href="/nueva">Cargar Reserva</a>
                    </li>                 
                <% } %>         
                <% if (locals.isLogged) { %>
                    <div class="dropdown">
                        <a class="nav-link fs-3 dropdown-toggle" href="#" id="navbarDropdownGastos" role="button"
                            data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Gastos
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownGastos">
                            <a class="dropdown-item" href="/agregar/gastos">Agregar Gastos</a>
                            <% if (locals.userLogged.admin == "full-admin") { %>
                                <a class="dropdown-item" href="/all/gastos">Ver Gastos</a>
                            <% } else { %>
                            <a class="dropdown-item" href="/ver/gastos">Ver Gastos</a>
                            <% } %>
                        </div>
                    </div>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
